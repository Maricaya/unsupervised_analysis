#!/bin/bash
# Bash script for rule gene_2_symbol

# Input files
input_files=(counts="{prefix}.tsv",)

# Output files
output_files=(symbol="{prefix}.symbol.tsv",)

# Log file
log_file="logs/gene2symbol/{prefix}.log",

